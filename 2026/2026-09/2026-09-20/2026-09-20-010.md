# 2026-09-20-010

## Summary

The workstation provisioning scripts
([ `windows/setup/Setup-Workstation.ps1`](file:///C:/usr/src/df/powershell/df-powershell/windows/setup/Setup-Workstation.ps1)
and root wrapper
[`Setup-Workstation.ps1`](file:///C:/usr/src/df/powershell/df-powershell/Setup-Workstation.ps1))
have been updated to support custom component configuration via `--override` for
**Visual Studio 2022 Community**.

---

### Key Changes Implemented

1. **Integrated VSConfig Override:**
    - Visual Studio 2022 Community is configured to install with:
        ```powershell
        winget install --id Microsoft.VisualStudio.2022.Community --override "--config `"C:\Setup\vs-components-full.vsconfig`" --passive --norestart"
        ```
    - In
      [ `windows/setup/Setup-Workstation.ps1`](file:///C:/usr/src/df/powershell/df-powershell/windows/setup/Setup-Workstation.ps1),
      the package catalog entry sets:
        ```powershell
        Override = if (-not [string]::IsNullOrWhiteSpace($VSConfigFile)) {
            "--config `"$VSConfigFile`" --passive --norestart"
        }
        else {
            '--passive --norestart'
        }
        ```

2. **Configurable `-VSConfigFile` Parameter:**
    - Both
      [`Setup-Workstation.ps1`](file:///C:/usr/src/df/powershell/df-powershell/Setup-Workstation.ps1)
      and
      [ `windows/setup/Setup-Workstation.ps1`](file:///C:/usr/src/df/powershell/df-powershell/windows/setup/Setup-Workstation.ps1)
      expose an optional parameter:
        ```powershell
        [Parameter()]
        [string]$VSConfigFile = 'C:\Setup\vs-components-full.vsconfig'
        ```
    - If a custom config file path is needed, it can be passed via:
        ```powershell
        .\Setup-Workstation.ps1 -VSConfigFile "C:\CustomPath\vs-components.vsconfig"
        ```

3. **Multi-Source Idempotency Maintained:**
    - If Visual Studio Community 2022 is **already installed** on the system,
      `Test-IsApplicationInstalled` detects it via:
        1. `winget list --exact --id Microsoft.VisualStudio.2022.Community`
        2. Filesystem check (`devenv.exe` in
           `Program Files\Microsoft Visual Studio\2022\Community\Common7\IDE`)
        3. Registry check (`*Visual Studio Community 2022*` under `Uninstall`
           keys)
        4. System PATH command (`devenv.exe`)
    - It outputs:
        ```text
        [SKIP] Visual Studio Community 2022 (v17.14.41) is already installed [WinGet]. Skipping reinstallation.
        ```
        and immediately returns without making any download or installation
        attempt.

4. **Validation & Quality Checks:**
    - **PSScriptAnalyzer:** Passed with `0` errors and `0` warnings against
      [ `.psscriptanalyzer.psd1`](file:///C:/usr/src/df/powershell/df-powershell/.psscriptanalyzer.psd1).
    - **Simulation (`-WhatIf`):** Verified clean simulation without errors.
    - **Documentation:** Updated and formatted in
      [ `windows/setup/README.md`](file:///C:/usr/src/df/powershell/df-powershell/windows/setup/README.md).
