# 2026-09-17-002

## Summary

The old faulty VM (`A731E1CB-...`) with the broken checkpoints is **already
gone**!

What happened is that `New-VM` was executed twice:

1. `1521ebac-0479-4c8e-aaa4-415b025b005e` (created at 12:10 AM)
2. `588769d1-20f9-41c2-8ba1-04beb0cff6c1` (created at 12:13 AM)

Both have **0 checkpoints** and point to the same VHDX. The second one
(`588769d1-...`) is the one created with the full processor and firmware
settings block.

---

### Step 1: Compare Both Configurations (Optional Quick Check)

To confirm which one has your desired CPU/RAM/Switch settings:

```powershell
Get-VM -Name "se.windows.hyperv.zeus.df.io" | Select-Object Id, Generation, ProcessorCount, MemoryStartup, @{ N = "Switch"; E = { (Get-VMNetworkAdapter -VM $_).SwitchName } }, @{ N = "SecureBoot"; E = { (Get-VMFirmware -VM $_).SecureBoot } } | Format-Table -AutoSize
```

---

### Step 2: Remove the Duplicate Older VM

Remove the first one (`1521ebac-0479-4c8e-aaa4-415b025b005e`). As always,
`Remove-VM` only deletes the VM configuration in Hyper-V; it **never** touches
the `.vhdx`:

```powershell
Get-VM -Id "1521ebac-0479-4c8e-aaa4-415b025b005e" | Remove-VM -Force
```

---

### Step 3: Clear any Partial Backup Folder & Run Backup

1. Remove any empty or partial folder from previous failed attempts:

    ```powershell
    if (Test-Path "D:\var\hyper\backup\se.windows.hyperv.zeus.df.io") {
        Remove-Item "D:\var\hyper\backup\se.windows.hyperv.zeus.df.io" -Recurse -Force
    }
    ```

2. Run the backup:
    ```powershell
    .\hyperv.ps1 -Backup -VMNames "se.windows.hyperv.zeus.df.io" -BackupRoot "D:\var\hyper\backup"
    ```

Because only one clean VM with 0 checkpoints remains, `Export-VM` will now
export smoothly.
