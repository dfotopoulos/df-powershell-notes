# 2026-09-17-006

## Summary

Root cause: `VSCodium.exe` is the graphical executable. It opened VSCodium and
returned before extension installation completed, so verification ran
immediately and failed.

The script now uses the dedicated blocking CLI:

```text
C:\Program Files\VSCodium\bin\codium.cmd
```

`VSCodium.exe` remains only as the interactive Start Menu shortcut target.

You can close the VSCodium window opened by the failed attempt, then rerun from
elevated PowerShell:

```powershell
Set-Location 'C:\usr\src\df\prog-2026-2027'
Set-ExecutionPolicy -Scope Process Bypass
.\prepare-vscodium-shared.ps1
```

No directory cleanup is needed.

A successful run should display the actual installation progress, followed by:

```text
ms-toolsai.jupyter
Machine-wide executable: C:\Program Files\VSCodium\VSCodium.exe
Machine-wide CLI: C:\Program Files\VSCodium\bin\codium.cmd
Shared extensions: C:\ProgramData\prog-2026-2027\vscodium-extensions
Installed extension: ms-toolsai.jupyter
```

The dedicated CLI was tested successfully with the shared extension directory
and returned exit code 0 without opening VSCodium. All affected verification
commands and documentation now use `codium.cmd`.
