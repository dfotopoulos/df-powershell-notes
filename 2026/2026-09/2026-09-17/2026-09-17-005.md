# 2026-09-17-005

## Summary

Root cause: VSCodium was already running. Its CLI forwarded the installation
request to an existing editor process using the normal per-user extension
directory instead of the requested shared directory. It returned success without
installing anything under ProgramData.

The corrected
[prepare-vscodium-shared.ps1](C:\usr\src\df\prog-2026-2027\prepare-vscodium-shared.ps1)
now:

- Uses a unique temporary `--user-data-dir`.
- Forces an isolated VSCodium maintenance instance.
- Displays the extension-installation output.
- Checks the native exit code.
- Verifies `ms-toolsai.jupyter` using `--list-extensions`.
- Verifies that the extension physically exists in the shared directory.
- Removes the temporary maintenance data afterward.
- Remains safe to rerun.

Run again from elevated PowerShell 7:

```powershell
Set-Location 'C:\usr\src\df\prog-2026-2027'
Set-ExecutionPolicy -Scope Process Bypass
.\prepare-vscodium-shared.ps1
```

You may leave existing VSCodium windows open. No manual cleanup is required.

A successful run should show installation output followed by:

```text
ms-toolsai.jupyter
Machine-wide executable: C:\Program Files\VSCodium\VSCodium.exe
Shared extensions: C:\ProgramData\prog-2026-2027\vscodium-extensions
Installed extension: ms-toolsai.jupyter
Shared Start Menu shortcut: ...
```

The correction passed PowerShell parser and safeguard validation. The root-cause
analysis is recorded in the administrator runbook, PowerShell walkthrough, and
executed-command log.
